前端算法课

每节课内容查看课程对应的分支

特点：
    1. 物理空间上不连续
    2. 每存放一个值，都要多开销一个引用空间
    3. 每一个节点，都认为自己是根节点

优点：
    1. 只要内存够大，就能存下，不用担心空间碎片问题
    2. 链表的添加和删除非常容易

缺点：
    1. 查询速度慢（指查询某个位置）
    2. 链表的每个节点都需要创建一个指向 next的引用，浪费一些空间。（也可以避免，当节点内数据越多的时候，这部分多开销的内存影响越小）