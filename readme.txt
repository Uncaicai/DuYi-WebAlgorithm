前端算法课

每节课内容查看课程对应的分支

js 中数组是定长的，初始化的时候决定，每次扩容都会造成性能消耗

数组特性： 
    1. 存储在物理空间上是连续的。
    2. 底层的数组长度是不可变的（可变是因为 js 引擎帮我们自动扩容）。
    3. 数组的变量，只想数组第一个元素的位置

优点：查询性能好
缺点：
    1. 空间必须得是连续的，所以如果数组比较大，容易存不下。（当系统的空间碎片较多的时候，容易存不下）
    2. 因为数组的长度是固定的，所以数组的内容难以被添加和删除


链表和数组的优缺点是互补的